#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	float f;
	float A;
	printf("Enter the side of the square:",f);
	scanf("%f",&f);
	
	A= (f*f);
	
	printf("The area of the square is %f",A,f);
	
	return 0;
}
